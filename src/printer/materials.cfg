# materials.cfg — filament materials offered in the KlipperScreen manual
# assignment dropdown (used when a lane isn't tracked in Spoolman).
#
# One material per line. Lines starting with # are comments and blank lines
# are ignored. Edit this list to add/remove materials — no code change
# needed, the panel re-reads this file each time the manual assign screen
# opens.
#
# "Other" is handled specially by the panel (reveals a free-text field) —
# keep it as the last entry.

PLA
PETG
ABS
ASA
TPU
Nylon
PC
Other
