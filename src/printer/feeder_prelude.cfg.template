# =============================================================================
# feeder.cfg — Bowden feeder MCU, steppers, buffer sync, and load macros
# =============================================================================
# Generated by Tool Feeder's installer for @@NUM_TOOLS@@ tool(s).
#
# Per-tool pin assignments below are placeholders (CHANGE_ME_*) — the
# installer can't know your wiring, so fill these in to match your actual
# feeder board and toolhead boards before using this config. See the
# "Next steps" printed at the end of install for the full checklist.
#
# Only this file needs to be [include]d from printer.cfg — it pulls in
# tool_feeder_macros.cfg itself.
# =============================================================================

[include tool_feeder_macros.cfg]


# -----------------------------------------------------------------------------
# MCU
# -----------------------------------------------------------------------------

[mcu feeder]
serial: @@FEEDER_SERIAL@@


# -----------------------------------------------------------------------------
# Per-tool steppers, unload buttons, and buffer sync
# -----------------------------------------------------------------------------
